#!/bin/sh

[ -x /etc/init.d/nikki-dns-failopen ] || exit 0
[ -x /etc/init.d/nikki ] || exit 0

/etc/init.d/nikki-dns-failopen enable >/dev/null 2>&1 || true
/etc/init.d/nikki-dns-failopen start >/dev/null 2>&1 || true

exit 0
