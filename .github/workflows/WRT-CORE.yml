# SPDX-License-Identifier: MIT
# Copyright (C) 2026 VIKINGYFY

#云编译公用核心
name: WRT-CORE

on:
  workflow_call:
    inputs:
      WRT_CONFIG:
        required: true
        type: string
      WRT_THEME:
        required: true
        type: string
      WRT_NAME:
        required: true
        type: string
      WRT_SSID:
        required: true
        type: string
      WRT_IP:
        required: true
        type: string
      WRT_PW:
        required: true
        type: string
      WRT_REPO:
        required: true
        type: string
      WRT_BRANCH:
        required: true
        type: string
      WRT_COMMIT:
        required: false
        type: string
        default: ''
      WRT_SOURCE:
        required: false
        type: string
      WRT_PACKAGE:
        required: false
        type: string
      WRT_TEST:
        required: false
        type: string
      WRT_BUILD_ONLY:
        required: false
        type: boolean
        default: false
      WRT_SPLIT_DEVICE_ARTIFACTS:
        required: false
        type: boolean
        default: true
      WRT_EXPECTED_DEVICE:
        required: false
        type: string
        default: ''
      WRT_LAN_TAILNET:
        required: false
        type: boolean
        default: true
      WRT_EMMC_DATA_PROVISIONING:
        required: false
        type: boolean
        default: false
      WRT_CONTAINER_RUNTIME_TEST:
        required: false
        type: boolean
        default: false
      WRT_CONTAINER_RUNTIME_MODE:
        required: false
        type: string
        default: 'prebuilt'
      WRT_CONTAINER_RUNTIME_VERSION:
        required: false
        type: string
        default: ''
      WRT_CPE_5G:
        required: false
        type: boolean
        default: false
      WRT_FEATURE_OVERLAY:
        required: false
        type: boolean
        default: true
      WRT_TAILSCALE_ROUTE_RECONCILE:
        required: false
        type: boolean
        default: true
      WRT_REQUIRED_DEVICE:
        required: false
        type: string
        default: ''
      WRT_WAN_SSH:
        required: false
        type: boolean
        default: false
      WRT_WAN_SSH_PORT:
        required: false
        type: string
        default: '22'
      WRT_WAN_SSH_SOURCE:
        required: false
        type: string
      WRTBAK_DEVICE_ALIAS:
        required: false
        type: string
        default: ''
      WRTBAK_PROXY_PROFILE:
        required: false
        type: string
        default: 'auto'
      WRTBAK_SITE:
        required: false
        type: string
        default: ''
      WRTBAK_PROXY_ARTIFACTS_ENABLED:
        required: false
        type: string
        default: '1'
      WRTBAK_PROXY_UPDATE_MODE:
        required: false
        type: string
        default: 'review-required'
      WRTBAK_FIRSTBOOT_AUTO_ENABLED:
        required: false
        type: string
        default: '0'
      WRTBAK_FIRSTBOOT_AUTO_TARGET:
        required: false
        type: string
        default: 's3'
      WRTBAK_FIRSTBOOT_AUTO_ATTEMPTS:
        required: false
        type: string
        default: '18'
      WRTBAK_FIRSTBOOT_AUTO_SLEEP:
        required: false
        type: string
        default: '10'
      WRTBAK_FIRSTBOOT_AUTO_REBOOT:
        required: false
        type: string
        default: '1'
      CI_NAME:
        required: false
        type: string
      WRT_ARCH:
        required: false
        type: string
      RUNNER_LABELS:
        required: false
        type: string
        default: '["ubuntu-latest"]'
      JOB_TIMEOUT_MINUTES:
        required: false
        type: number
        default: 360
      DEBUG_SSH:
        required: false
        type: boolean
        default: false
    secrets:
      HEADSCALE_OPENWRT_AUTHKEY:
        required: false
      HEADSCALE_CI_AUTHKEY:
        required: false
      HEADSCALE_URL:
        required: false
      MULTICA_TOKEN:
        required: false
      MULTICA_SERVER_URL:
        required: false
      MULTICA_APP_URL:
        required: false
      MULTICA_WORKSPACE_ID:
        required: false
      OPENWRT_DROPBEAR_AUTHORIZED_KEYS:
        required: false
      WRTBAK_R2_ENDPOINT:
        required: false
      WRTBAK_R2_REGION:
        required: false
      WRTBAK_R2_BUCKET:
        required: false
      WRTBAK_R2_PREFIX:
        required: false
      WRTBAK_R2_ACCESS_KEY_ID:
        required: false
      WRTBAK_R2_SECRET_ACCESS_KEY:
        required: false
      WRTBAK_HOME_PROXY_URL:
        required: false
      WRTBAK_OFFICE_PROXY_URL:
        required: false
      WRTBAK_DORM_PROXY_URL:
        required: false
      COMMANDCODE_API_KEY:
        required: false
      CLIPROXYAPI_API_KEY:
        required: false
      CLIPROXYAPI_BASE_URL:
        required: false
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  WRT_CONFIG: ${{inputs.WRT_CONFIG}}
  WRT_THEME: ${{inputs.WRT_THEME}}
  WRT_NAME: ${{inputs.WRT_NAME}}
  WRT_SSID: ${{inputs.WRT_SSID}}
  WRT_IP: ${{inputs.WRT_IP}}
  WRT_PW: ${{inputs.WRT_PW}}
  WRT_REPO: ${{inputs.WRT_REPO}}
  WRT_BRANCH: ${{inputs.WRT_BRANCH}}
  WRT_COMMIT: ${{inputs.WRT_COMMIT}}
  WRT_SOURCE: ${{inputs.WRT_SOURCE}}
  WRT_PACKAGE: ${{inputs.WRT_PACKAGE}}
  WRT_TEST: ${{inputs.WRT_TEST}}
  WRT_BUILD_ONLY: ${{inputs.WRT_BUILD_ONLY}}
  WRT_SPLIT_DEVICE_ARTIFACTS: ${{inputs.WRT_SPLIT_DEVICE_ARTIFACTS}}
  WRT_EXPECTED_DEVICE: ${{inputs.WRT_EXPECTED_DEVICE}}
  WRT_LAN_TAILNET: ${{inputs.WRT_LAN_TAILNET}}
  WRT_EMMC_DATA_PROVISIONING: ${{inputs.WRT_EMMC_DATA_PROVISIONING}}
  WRT_CONTAINER_RUNTIME_TEST: ${{inputs.WRT_CONTAINER_RUNTIME_TEST}}
  WRT_CONTAINER_RUNTIME_MODE: ${{inputs.WRT_CONTAINER_RUNTIME_MODE}}
  WRT_CONTAINER_RUNTIME_VERSION: ${{inputs.WRT_CONTAINER_RUNTIME_VERSION}}
  WRT_CPE_5G: ${{inputs.WRT_CPE_5G}}
  WRT_FEATURE_OVERLAY: ${{inputs.WRT_FEATURE_OVERLAY}}
  WRT_TAILSCALE_ROUTE_RECONCILE: ${{inputs.WRT_TAILSCALE_ROUTE_RECONCILE}}
  WRT_REQUIRED_DEVICE: ${{inputs.WRT_REQUIRED_DEVICE}}
  WRT_WAN_SSH: ${{inputs.WRT_WAN_SSH}}
  WRT_WAN_SSH_PORT: ${{inputs.WRT_WAN_SSH_PORT}}
  WRT_WAN_SSH_SOURCE: ${{inputs.WRT_WAN_SSH_SOURCE}}
  WRTBAK_DEVICE_ALIAS: ${{inputs.WRTBAK_DEVICE_ALIAS}}
  WRTBAK_PROXY_PROFILE: ${{inputs.WRTBAK_PROXY_PROFILE}}
  WRTBAK_SITE: ${{inputs.WRTBAK_SITE}}
  WRTBAK_PROXY_ARTIFACTS_ENABLED: ${{inputs.WRTBAK_PROXY_ARTIFACTS_ENABLED}}
  WRTBAK_PROXY_UPDATE_MODE: ${{inputs.WRTBAK_PROXY_UPDATE_MODE}}
  WRTBAK_FIRSTBOOT_AUTO_ENABLED: ${{inputs.WRTBAK_FIRSTBOOT_AUTO_ENABLED}}
  WRTBAK_FIRSTBOOT_AUTO_TARGET: ${{inputs.WRTBAK_FIRSTBOOT_AUTO_TARGET}}
  WRTBAK_FIRSTBOOT_AUTO_ATTEMPTS: ${{inputs.WRTBAK_FIRSTBOOT_AUTO_ATTEMPTS}}
  WRTBAK_FIRSTBOOT_AUTO_SLEEP: ${{inputs.WRTBAK_FIRSTBOOT_AUTO_SLEEP}}
  WRTBAK_FIRSTBOOT_AUTO_REBOOT: ${{inputs.WRTBAK_FIRSTBOOT_AUTO_REBOOT}}
  WRT_DIR: 'wrt'
  CI_NAME: ${{inputs.CI_NAME}}
  WRT_ARCH: ${{inputs.WRT_ARCH}}
  DEBUG_SSH: ${{inputs.DEBUG_SSH}}
  WRT_PRIVATE_BUILD: 'false'
  WRT_PRIVATE_BUILD_REASON: ''
  WRT_ARTIFACT_PRIVACY_SUFFIX: public
  # Build-cache governance knobs. Validated by Scripts/wrt_cache_reclaim.sh.
  # Caches are retained while free space is above the low-water mark; below it
  # reclamation is ordered and bounded and never touches staging/build/dl/tools.
  WRT_CACHE_LOW_WATER_GB: '12'
  WRT_CACHE_TARGET_FREE_GB: '20'
  WRT_CCACHE_MAX_SIZE: '5G'
  WRT_GO_BUILD_MAX: '2G'
  WRT_GOMOD_MAX: '3G'

jobs:
  core:
    name: ${{inputs.WRT_SOURCE}}
    runs-on: ${{ fromJSON(inputs.RUNNER_LABELS) }}
    timeout-minutes: ${{ inputs.JOB_TIMEOUT_MINUTES }}
    permissions:
      contents: write
      actions: write
    steps:
      - name: Install BC
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          if command -v sudo >/dev/null 2>&1; then
            SUDO="sudo -E"
          else
            SUDO=""
          fi
          # bc is preinstalled on GitHub-hosted runners; bound this anyway so
          # a stalled apt mirror cannot wedge the step before the IE 60-min
          # hard bound even starts.
          timeout 300 ${SUDO} apt-get -yqq \
            -o Acquire::Retries=2 \
            -o Acquire::http::Timeout=30 \
            -o Acquire::https::Timeout=30 \
            install bc \
            || echo "::warning::bc install failed or timed out (preinstalled on hosted runners)"

      - name: Free Disk Space
        if: ${{ !contains(inputs.RUNNER_LABELS, 'self-hosted') }}
        uses: endersonmenezes/free-disk-space@713d134e243b926eba4a5cce0cf608bfd1efb89a # v2.1.1
        with:
          remove_android: true
          remove_dotnet: true
          remove_haskell: true
          remove_tool_cache: true
          remove_swap: true
          remove_packages: "azure-cli google-cloud-cli microsoft-edge-stable google-chrome-stable firefox postgresql* temurin-* *llvm* mysql* dotnet-sdk-*"
          remove_packages_one_command: true
          remove_folders: "/usr/share/swift /usr/share/miniconda /usr/share/az* /usr/share/glade* /usr/local/share/chromium /usr/local/share/powershell"
          testing: false

      - name: Checkout Projects
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.1.0

      - name: Validate LAN IP
        run: |
          LAN_IP="${WRT_IP:-192.168.10.1}"
          "$GITHUB_WORKSPACE/Scripts/ValidateLanIp.sh" "$LAN_IP"
          echo "WRT_IP=$LAN_IP" >> "$GITHUB_ENV"

      - name: Initialization Environment
        timeout-minutes: 75
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          if command -v sudo >/dev/null 2>&1; then
            SUDO="sudo -E"
          else
            SUDO=""
          fi

          # Single 60-minute hard bound over the WHOLE environment bootstrap
          # (firefox purge, Ubuntu mirror normalization, apt update,
          # dependency install and init_build_environment.sh). Every apt call
          # inside goes through Scripts/ci-apt-lib.sh (apt-get only, bounded
          # per-command timeout, bounded retries, single official-archive
          # fallback). A stalled apt mirror now fails this step loudly within
          # 60 minutes instead of wedging the runner for the ~1h15m GitHub
          # previously took to declare it lost. Sized against the longest
          # successful baseline run (RE-SS-01 Initialization Environment, 23
          # min) with headroom; step timeout-minutes (75) exceeds the inner
          # bound so the outer kill can surface first.
          timeout --kill-after=30 3600 bash "$GITHUB_WORKSPACE/Scripts/ci_init_environment.sh" \
            || { echo "::error::Initialization Environment failed or exceeded the 60min hard timeout"; exit 1; }
          if command -v go >/dev/null 2>&1 && go version 2>/dev/null | grep -Eq 'go1\.(2[6-9]|[3-9][0-9])(\.|$)'; then
            echo "GO_INSTALLED=true" >> "$GITHUB_ENV"
          else
            echo "GO_INSTALLED=false" >> "$GITHUB_ENV"
          fi
          if command -v systemctl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
            timeout --kill-after=30 30 ${SUDO} systemctl daemon-reload || echo "::warning::systemctl daemon-reload skipped (timeout/failure)"
          fi
          if command -v timedatectl >/dev/null 2>&1 && [ -d /run/systemd/system ]; then
            timeout --kill-after=30 30 ${SUDO} timedatectl set-timezone "Asia/Shanghai" || echo "::warning::Unable to set runner timezone; continuing with explicit TZ timestamps."
          fi
#          sudo chown $USER:$GROUPS $GITHUB_WORKSPACE
#          sudo mkdir -p /mnt/build_wrt
#          sudo chown $USER:$USER /mnt/build_wrt
#          sudo ln -s /mnt/build_wrt $GITHUB_WORKSPACE/wrt

      - name: Setup Go
        if: env.GO_INSTALLED != 'true'
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: '1.26'

      - name: Check Go
        run: go version

      - name: Initialization Values
        run: |
          echo "WRT_DATE=$(TZ=UTC-8 date +"%y.%m.%d-%H.%M.%S")" >> $GITHUB_ENV
          echo "WRT_MARK=${GITHUB_REPOSITORY%%/*}" >> $GITHUB_ENV
          echo "WRT_TARGET=$(grep -m 1 -oP '^CONFIG_TARGET_\K[\w]+(?=\=y)' ./Config/$WRT_CONFIG.txt)" >> $GITHUB_ENV
          WRT_INFO_VALUE="${WRT_SOURCE%%/*}"
          [ -n "$WRT_INFO_VALUE" ] || WRT_INFO_VALUE="$(grep -m 1 -oP '^CONFIG_TARGET_\K[\w]+(?=\=y)' ./Config/$WRT_CONFIG.txt)"
          echo "WRT_INFO=$WRT_INFO_VALUE" >> $GITHUB_ENV
          echo "WRT_KVER=none" >> $GITHUB_ENV
          echo "WRT_LIST=none" >> $GITHUB_ENV

          echo "WRT_CI=$WRT_CI" >> $GITHUB_ENV
          [[ -z $WRT_ARCH ]] && {
            export WRT_ARCH=$(sed -n 's/.*_DEVICE_\(.*\)_DEVICE_.*/\1/p' $GITHUB_WORKSPACE/Config/$WRT_CONFIG.txt | head -n 1)
            echo "WRT_ARCH=$WRT_ARCH" >> $GITHUB_ENV
          }

      - name: Configure Go Module Access
        run: |
          echo "GOPROXY=https://proxy.golang.org|https://goproxy.cn|direct" >> $GITHUB_ENV
          echo "GOSUMDB=sum.golang.org" >> $GITHUB_ENV
          echo "GOPROXY=https://proxy.golang.org|https://goproxy.cn|direct"
          echo "GOSUMDB=sum.golang.org"
      - name: Clone Code
        run: |
          . $GITHUB_WORKSPACE/Scripts/retry.sh
          retry_cmd 5 15 git clone --depth=1 --single-branch --branch $WRT_BRANCH $WRT_REPO ./wrt/

          if [ -n "$WRT_COMMIT" ]; then
            if [ "${#WRT_COMMIT}" -ne 40 ] || printf '%s' "$WRT_COMMIT" | grep -Eq '[^0-9a-fA-F]'; then
              echo "ERROR: WRT_COMMIT must be a full 40-character hexadecimal commit SHA" >&2
              exit 1
            fi

            retry_cmd 5 15 git -C ./wrt fetch --depth=1 origin "$WRT_COMMIT"
            (
              cd ./wrt
              git checkout --detach "$WRT_COMMIT"
              resolved_commit="$(git rev-parse HEAD)"
              requested_commit="$(printf '%s' "$WRT_COMMIT" | tr 'A-F' 'a-f')"
              if [ "$resolved_commit" != "$requested_commit" ]; then
                echo "ERROR: requested source commit $requested_commit but checked out $resolved_commit" >&2
                exit 1
              fi
            )
          fi

          $GITHUB_WORKSPACE/Scripts/patch_libiwrt_k612_cacheline_assert.sh "./wrt" "$WRT_SOURCE" "$WRT_BRANCH"
          $GITHUB_WORKSPACE/Scripts/fix_qca8k_assisted_learning_patch.sh "./wrt"

          WRT_HASH_VAL="$(git -C ./wrt rev-parse HEAD)"
          echo "WRT_HASH=$WRT_HASH_VAL" >> $GITHUB_ENV
          echo "$WRT_REPO/$WRT_BRANCH/$WRT_HASH_VAL" > "$GITHUB_WORKSPACE/repo_flag"

          # Remove mainland mirrors so build source resolution stays consistent across runners.
          PROJECT_MIRRORS_FILE="./scripts/projectsmirrors.json"
          if [ -f "$PROJECT_MIRRORS_FILE" ]; then
            sed -i '/.cn\//d; /tencent/d; /aliyun/d' "$PROJECT_MIRRORS_FILE"
          fi

      - name: Check Scripts
        run: |
          find ./ -maxdepth 3 -type f -iregex ".*\(txt\|sh\)$" -exec dos2unix {} \; -exec chmod +x {} \;

      - name: Repository Smoke Tests
        run: |
          for test_file in ./tests/test_*.sh; do
            bash "$test_file"
          done

      - name: Compute Build Cache Identity
        run: |
          set -euo pipefail
          . "$GITHUB_WORKSPACE/Scripts/wrt_cache_lib.sh"

          device_config="$GITHUB_WORKSPACE/Config/${WRT_CONFIG}.txt"
          # The manifest pairs a FIXED logical label with each input's content
          # digest. Absolute $GITHUB_WORKSPACE / $RUNNER_TEMP paths never enter
          # the hash, so identical inputs yield the same scope in any checkout
          # directory. The compatibility scope never rotates by date; run
          # uniqueness is appended only to the save key.
          manifest="$RUNNER_TEMP/wrt-cache-manifest"
          : > "$manifest"

          # Required repository/config/script files: fail closed if missing.
          add_file() {
            local label=$1 path=$2
            [ -s "$path" ] || {
              echo "ERROR: required cache identity input missing/empty: $label ($path)" >&2
              exit 1
            }
            printf '%s\t%s\n' "$label" "$(wrt_cache_file_digest "$path")" >> "$manifest"
          }
          # Normalized scalar dimensions: always represented, even when empty,
          # so a dimension can never silently disappear into a broad key.
          add_value() {
            local label=$1 value=$2
            local digest
            digest="$(printf '%s' "$value" | sha256sum | awk '{print $1}')"
            printf '%s\t%s\n' "$label" "$digest" >> "$manifest"
          }

          add_file source                       "$GITHUB_WORKSPACE/repo_flag"
          add_file config/general               "$GITHUB_WORKSPACE/Config/GENERAL.txt"
          add_file "config/device/${WRT_CONFIG}" "$device_config"
          add_file script/Packages.sh           "$GITHUB_WORKSPACE/Scripts/Packages.sh"
          add_file script/Handles.sh            "$GITHUB_WORKSPACE/Scripts/Handles.sh"
          add_file script/Settings.sh           "$GITHUB_WORKSPACE/Scripts/Settings.sh"
          add_value input/package               "${WRT_PACKAGE:-}"
          add_value container/test              "${WRT_CONTAINER_RUNTIME_TEST:-false}"
          add_value container/mode              "${WRT_CONTAINER_RUNTIME_MODE:-prebuilt}"
          add_value container/version           "${WRT_CONTAINER_RUNTIME_VERSION:-latest}"
          if [ "${WRT_CONTAINER_RUNTIME_TEST:-false}" = "true" ]; then
            overlay_digest="$(wrt_cache_tree_digest "$GITHUB_WORKSPACE/files-container-runtime-test")"
          else
            overlay_digest="$(printf '%s' disabled | sha256sum | awk '{print $1}')"
          fi
          printf '%s\t%s\n' container/overlay "$overlay_digest" >> "$manifest"

          [ -s "$manifest" ] || { echo "ERROR: empty cache identity manifest" >&2; exit 1; }
          cache_scope="$(wrt_cache_scope_from_manifest < "$manifest")"

          echo "---- build cache identity manifest (fixed label + content digest) ----"
          cat "$manifest"
          echo "--------------------------------------------------------------------"

          # WRT_ARCH is the device/job identity. run_id/run_attempt make the
          # save key unique per run; the restore prefix keeps the full scope as
          # an explicit "-run" boundary so it can never match another scope.
          mapfile -t cache_keys < <(
            wrt_cache_build_keys "$WRT_ARCH" "$cache_scope" \
              "$GITHUB_RUN_ID" "${GITHUB_RUN_ATTEMPT:-1}"
          )
          echo "WRT_CACHE_SCOPE=${cache_keys[0]}" >> "$GITHUB_ENV"
          echo "WRT_CACHE_RESTORE_PREFIX=${cache_keys[1]}" >> "$GITHUB_ENV"
          echo "WRT_CACHE_SAVE_KEY=${cache_keys[2]}" >> "$GITHUB_ENV"
          echo "Compatibility scope : ${cache_keys[0]}"
          echo "Restore prefix       : ${cache_keys[1]}  (same full scope only)"
          echo "Per-run save key     : ${cache_keys[2]}"

      - name: Restore Build Cache
        id: cache_restore
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          # Keep this allow-list identical to Scripts/wrt_cache_payload_guard.sh.
          # Only regenerable caches and host/toolchain staging are cached;
          # staging_dir/target-* (staged rootfs), build_dir, files and bin are
          # intentionally excluded.
          path: |
            ./wrt/.ccache
            ./wrt/staging_dir/host
            ./wrt/staging_dir/toolchain-*
            ./wrt/dl/go-mod-cache
            ./wrt/tmp/go-build
          # Unique per run, so an exact primary hit is the exception; a normal
          # warm build recovers the newest prior generation via restore-keys.
          key: ${{ env.WRT_CACHE_SAVE_KEY }}
          # Bounded prefix: it embeds the full compatibility scope hash, so a
          # different source SHA/config/device/input can never be restored.
          restore-keys: ${{ env.WRT_CACHE_RESTORE_PREFIX }}

      - name: Log Cache Restore Result
        env:
          CACHE_HIT: ${{ steps.cache_restore.outputs.cache-hit }}
          CACHE_RESTORED_KEY: ${{ steps.cache_restore.outputs.cache-primary-key }}
          SAVE_KEY: ${{ env.WRT_CACHE_SAVE_KEY }}
          RESTORE_PREFIX: ${{ env.WRT_CACHE_RESTORE_PREFIX }}
        run: |
          set -euo pipefail
          echo "========cache restore result========"
          echo "unique save key : $SAVE_KEY"
          echo "restore prefix  : $RESTORE_PREFIX"
          if [ "$CACHE_HIT" = "true" ]; then
            echo "restore state   : EXACT primary-key hit ($CACHE_RESTORED_KEY)"
          elif [ -n "$CACHE_RESTORED_KEY" ]; then
            echo "restore state   : PREFIX recovery of prior generation ($CACHE_RESTORED_KEY)"
            case "$CACHE_RESTORED_KEY" in
              "$RESTORE_PREFIX"*) echo "recovered key stays inside the bounded same-scope prefix" ;;
              *) echo "ERROR: recovered key escapes the bounded restore prefix" >&2; exit 1 ;;
            esac
          else
            echo "restore state   : MISS (cold build for this compatibility scope)"
          fi
          if [ -d ./wrt ]; then
            df -h ./wrt || true
            du -sh ./wrt/.ccache ./wrt/staging_dir/host ./wrt/staging_dir/toolchain-* \
              ./wrt/dl/go-mod-cache ./wrt/tmp/go-build 2>/dev/null || true
          fi

      - name: Refresh the cache
        run: |
          # Refresh mtimes of restored host/toolchain stamp files so OpenWrt
          # does not treat them as stale right after extraction. This only
          # updates timestamps; it does NOT prevent GitHub-side cache eviction
          # (service retention is unrelated to mtime) and never touches target/*.
          if [ -d "./wrt/staging_dir" ]; then
            find "./wrt/staging_dir" -type d -name "stamp" -not -path "*target*" | while read -r dir; do
              find "$dir" -type f -exec touch {} +
            done
          fi



      - name: Update Feeds
        run: |
          . $GITHUB_WORKSPACE/Scripts/retry.sh
          cd ./wrt/

          retry_cmd 5 15 ./scripts/feeds update -a
          retry_cmd 5 15 ./scripts/feeds install -a

      - name: Record Go Cache Path Evidence
        run: |
          # Evidence only: do NOT blindly override the feed's Go cache rules.
          # OpenWrt cross builds pin GOCACHE/GOMODCACHE in the golang feed
          # (often with GOENV=off), which can differ from the host `go env`.
          set -uo pipefail
          echo "---- host go env (NOT authoritative for OpenWrt cross builds) ----"
          go env GOCACHE GOMODCACHE GOENV 2>/dev/null || echo "host go unavailable"
          echo "---- OpenWrt golang feed build rules ----"
          golang_values="$(find ./wrt/feeds -path '*lang/golang/golang-values.mk' 2>/dev/null | head -n 1 || true)"
          if [ -n "$golang_values" ] && [ -f "$golang_values" ]; then
            echo "rules file: $golang_values"
            grep -nE 'GO_CACHE|GO_MOD_CACHE|GOCACHE|GOMODCACHE|GOENV' "$golang_values" || \
              echo "WARN: no GOCACHE/GOMODCACHE/GOENV token found in $golang_values"
          else
            echo "WARN: feeds lang/golang/golang-values.mk not found; cross-build GOCACHE/GOMODCACHE not verified from source"
          fi
          echo "Cached payload paths (expected: GOCACHE=./wrt/tmp/go-build GOMODCACHE=./wrt/dl/go-mod-cache)"

      - name: Setup Node.js for Agent Runtime Packaging
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.20.0
          check-latest: false
          registry-url: https://registry.npmjs.org

      - name: Custom Packages and Agent Runtimes
        id: custom_packages
        run: |
          cd ./wrt/package/

          $GITHUB_WORKSPACE/Scripts/Packages.sh
          $GITHUB_WORKSPACE/Scripts/Handles.sh
          cd $GITHUB_WORKSPACE
          mkdir -p ./wrt/files
          $GITHUB_WORKSPACE/Scripts/fetch_uv_runtime.sh "$GITHUB_WORKSPACE/wrt/files"
          $GITHUB_WORKSPACE/Scripts/fetch_node_runtime.sh "$GITHUB_WORKSPACE/wrt/files"
          $GITHUB_WORKSPACE/Scripts/fetch_opencode_runtime.sh "$GITHUB_WORKSPACE/wrt/files"
          $GITHUB_WORKSPACE/Scripts/fetch_multica_runtime.sh "$GITHUB_WORKSPACE/wrt/files"
          $GITHUB_WORKSPACE/Scripts/finalize_agent_runtime_baseline.sh "$GITHUB_WORKSPACE/wrt/files"
          $GITHUB_WORKSPACE/Scripts/fetch_viewturbocore.sh
          if [ "${WRT_CONTAINER_RUNTIME_TEST:-false}" = "true" ]; then
            case "${WRT_EXPECTED_DEVICE:-}" in
              jdcloud_re-ss-01|jdcloud_re-cs-02|jdcloud_re-cs-07) ;;
              *)
                echo "ERROR: container runtime is allowed only for jdcloud_re-ss-01, jdcloud_re-cs-02, and jdcloud_re-cs-07" >&2
                exit 1
                ;;
            esac
            case "${WRT_CONTAINER_RUNTIME_MODE:-prebuilt}" in
              prebuilt|auto)
                if ! $GITHUB_WORKSPACE/Scripts/fetch_container_runtime.sh "$GITHUB_WORKSPACE/wrt/files"; then
                  if [ "${WRT_CONTAINER_RUNTIME_MODE:-prebuilt}" = "auto" ]; then
                    echo "WARN: prebuilt container runtime fetch failed; falling back to OpenWrt packages"
                    echo "WRT_CONTAINER_RUNTIME_RESOLVED_MODE=openwrt" >> "$GITHUB_ENV"
                  else
                    echo "ERROR: prebuilt container runtime fetch failed" >&2
                    exit 1
                  fi
                else
                  echo "WRT_CONTAINER_RUNTIME_RESOLVED_MODE=prebuilt" >> "$GITHUB_ENV"
                fi
                ;;
              openwrt)
                echo "WRT_CONTAINER_RUNTIME_RESOLVED_MODE=openwrt" >> "$GITHUB_ENV"
                ;;
              *)
                echo "ERROR: WRT_CONTAINER_RUNTIME_MODE must be prebuilt, auto, or openwrt" >&2
                exit 1
                ;;
            esac
          fi
          if [ "${WRT_FEATURE_OVERLAY:-true}" = "true" ]; then
            cp -rf ./files/. ./wrt/files/
            if [ "${WRT_CONTAINER_RUNTIME_TEST:-false}" = "true" ]; then
              test -d ./files-container-runtime-test || {
                echo "ERROR: container runtime test overlay is missing" >&2
                exit 1
              }
              test "$(git ls-files --stage -- files-container-runtime-test/etc/init.d/containerd-test | awk '{print $1}')" = 100755 || {
                echo "ERROR: containerd test init script is not executable in Git" >&2
                exit 1
              }
              test "$(git ls-files --stage -- files-container-runtime-test/etc/uci-defaults/97-containerd-test-enable | awk '{print $1}')" = 100755 || {
                echo "ERROR: containerd test uci-defaults script is not executable in Git" >&2
                exit 1
              }
              cp -rf ./files-container-runtime-test/. ./wrt/files/
              chmod +x \
                ./wrt/files/etc/init.d/containerd-test \
                ./wrt/files/etc/uci-defaults/97-containerd-test-enable
            fi
            $GITHUB_WORKSPACE/Scripts/ConfigureCpe5G.sh "$GITHUB_WORKSPACE/wrt/files" "${WRT_CPE_5G:-false}"
            $GITHUB_WORKSPACE/Scripts/ConfigureLanTailnetGateway.sh "$GITHUB_WORKSPACE/wrt/files" "${WRT_LAN_TAILNET:-true}"
            $GITHUB_WORKSPACE/Scripts/ConfigureEmmcDataProvisioning.sh "$GITHUB_WORKSPACE/wrt/files" "${WRT_EMMC_DATA_PROVISIONING:-false}"
            $GITHUB_WORKSPACE/Scripts/ConfigureWanDropbearAccess.sh "$GITHUB_WORKSPACE/wrt/files" "${WRT_WAN_SSH:-false}" "${WRT_WAN_SSH_PORT:-22}" "${WRT_WAN_SSH_SOURCE:-}"
          fi

          # This small overlay is independent from the larger private feature
          # set. It keeps every Tailscale-enabled firmware covered, including
          # builds that intentionally disable CPE/Lucky/wrtbak extras.
          if [ "${WRT_TAILSCALE_ROUTE_RECONCILE:-true}" = "true" ]; then
            mkdir -p ./wrt/files/etc/config ./wrt/files/etc/init.d ./wrt/files/etc/uci-defaults ./wrt/files/usr/sbin
            cp -f ./files/etc/config/tailscale ./wrt/files/etc/config/tailscale
            cp -f ./files/etc/init.d/tailscale-route-reconcile ./wrt/files/etc/init.d/tailscale-route-reconcile
            cp -f ./files/etc/uci-defaults/96-tailscale-uci-fallback ./wrt/files/etc/uci-defaults/96-tailscale-uci-fallback
            cp -f ./files/etc/uci-defaults/99-tailscale-route-reconcile ./wrt/files/etc/uci-defaults/99-tailscale-route-reconcile
            cp -f ./files/usr/sbin/tailscale-route-reconcile ./wrt/files/usr/sbin/tailscale-route-reconcile
            chmod 0755 ./wrt/files/etc/init.d/tailscale-route-reconcile ./wrt/files/etc/uci-defaults/96-tailscale-uci-fallback ./wrt/files/etc/uci-defaults/99-tailscale-route-reconcile ./wrt/files/usr/sbin/tailscale-route-reconcile
            test -x ./wrt/files/usr/sbin/tailscale-route-reconcile || {
              echo "ERROR: tailscale route reconcile helper is missing from the firmware overlay" >&2
              exit 1
            }
            test -x ./wrt/files/etc/init.d/tailscale-route-reconcile || {
              echo "ERROR: tailscale route reconcile init script is missing from the firmware overlay" >&2
              exit 1
            }
          elif [ "${WRT_FEATURE_OVERLAY:-true}" != "true" ]; then
            echo "Feature overlay disabled: omitting CPE, Lucky, Tailscale/Headscale and wrtbak overlays"
          fi

          # Data runtime is a separate, safe baseline. It is injected into
          # every OpenWrt-CI image (including intentionally minimal images),
          # but destructive GPT provisioning remains disabled by its UCI gate.
          # The diagnostic helper is read-only; it never guesses a largest or
          # unlabeled partition.
          mkdir -p \
            ./wrt/files/etc/config \
            ./wrt/files/etc/init.d \
            ./wrt/files/etc/multica \
            ./wrt/files/etc/profile.d \
            ./wrt/files/etc/rclone-data-backup \
            ./wrt/files/etc/crontabs \
            ./wrt/files/etc/uci-defaults \
            ./wrt/files/usr/sbin
          nikki_package_init="$(find ./wrt/package -type f -path '*/luci-app-nikki/root/etc/init.d/nikki' -print -quit)"
          if [ -n "$nikki_package_init" ]; then
            grep -q '^START=99$' "$nikki_package_init" || {
              echo "ERROR: Nikki init START must remain 99 for fail-open startup ordering" >&2
              exit 1
            }
          else
            echo "Nikki package init is absent for this target; skipping its startup-order assertion"
          fi
          # A reviewed RE workflow may already have enabled this UCI gate
          # after the full overlay was copied. Preserve that explicit
          # per-device decision; minimal images receive the safe default.
          if [ ! -f ./wrt/files/etc/config/agent-storage ]; then
            cp -f ./files/etc/config/agent-storage ./wrt/files/etc/config/agent-storage
          fi
          cp -f ./files/etc/config/rclone-data-backup ./wrt/files/etc/config/rclone-data-backup
          cp -f ./files/etc/config/nikki-dns-failopen ./wrt/files/etc/config/nikki-dns-failopen
          cp -f ./files/etc/crontabs/root ./wrt/files/etc/crontabs/root
          if [ -n "${WRTBAK_DEVICE_ALIAS:-}" ]; then
            case "$WRTBAK_DEVICE_ALIAS" in
              *[!A-Za-z0-9._-]* )
                echo "ERROR: WRTBAK_DEVICE_ALIAS is not safe for the data backup namespace" >&2
                exit 1
                ;;
            esac
            sed -i "s/^\(\toption device_alias \).*/\1'$WRTBAK_DEVICE_ALIAS'/" ./wrt/files/etc/config/rclone-data-backup
          fi
          cp -f ./files/etc/data-runtime.env ./wrt/files/etc/data-runtime.env
          cp -f ./files/etc/init.d/data-runtime ./wrt/files/etc/init.d/data-runtime
          cp -f ./files/etc/init.d/emmc-data-provision ./wrt/files/etc/init.d/emmc-data-provision
          cp -f ./files/etc/init.d/rclone-data-backup ./wrt/files/etc/init.d/rclone-data-backup
          cp -f ./files/etc/init.d/nikki-dns-failopen ./wrt/files/etc/init.d/nikki-dns-failopen
          cp -f ./files/etc/multica/openwrt-agent.md ./wrt/files/etc/multica/openwrt-agent.md
          cp -f ./files/etc/uci-defaults/93-logd-ring-size ./wrt/files/etc/uci-defaults/93-logd-ring-size
          cp -f ./files/etc/uci-defaults/95-multica-maintenance-cron ./wrt/files/etc/uci-defaults/95-multica-maintenance-cron
          cp -f ./files/etc/uci-defaults/99-enable-nikki-dns-failopen ./wrt/files/etc/uci-defaults/99-enable-nikki-dns-failopen
          mkdir -p ./wrt/files/etc/agent-tools
          cp -f ./files/etc/agent-tools/README.md ./wrt/files/etc/agent-tools/README.md
          mkdir -p ./wrt/files/etc/openwrt-ci
          if [ -n "${GITHUB_SHA:-}" ]; then
            printf '%s\n' "$GITHUB_SHA" > ./wrt/files/etc/openwrt-ci/firmware-commit
          else
            cp -f ./files/etc/openwrt-ci/firmware-commit ./wrt/files/etc/openwrt-ci/firmware-commit
          fi
          cp -f ./files/etc/profile.d/99-data-runtime.sh ./wrt/files/etc/profile.d/99-data-runtime.sh
          cp -f ./files/etc/rclone-data-backup/include.conf ./wrt/files/etc/rclone-data-backup/include.conf
          cp -f ./files/etc/uci-defaults/94-zram-swap-defaults ./wrt/files/etc/uci-defaults/94-zram-swap-defaults
          cp -f ./files/etc/uci-defaults/98-provision-emmc-data ./wrt/files/etc/uci-defaults/98-provision-emmc-data
          cp -f ./files/etc/uci-defaults/99-auto-mount-data ./wrt/files/etc/uci-defaults/99-auto-mount-data
          cp -f ./files/etc/uci-defaults/99-enable-data-runtime ./wrt/files/etc/uci-defaults/99-enable-data-runtime
          # First-boot defaults can be interrupted after GPT creation but
          # before mkfs. Install the same reviewed workers under stable names
          # so an enabled init service can resume only that pending marker.
          cp -f ./files/etc/uci-defaults/98-provision-emmc-data ./wrt/files/usr/sbin/emmc-data-provision
          cp -f ./files/etc/uci-defaults/99-auto-mount-data ./wrt/files/usr/sbin/auto-mount-data
          cp -f ./files/usr/sbin/multica-device-profile ./wrt/files/usr/sbin/multica-device-profile
          cp -f ./files/usr/sbin/openwrt-data-storage-diagnose ./wrt/files/usr/sbin/openwrt-data-storage-diagnose
          cp -f ./files/usr/sbin/pi-append-system-link ./wrt/files/usr/sbin/pi-append-system-link
          cp -f ./files/usr/sbin/commandcode-role-link ./wrt/files/usr/sbin/commandcode-role-link
          cp -f ./files/usr/sbin/opencode-role-link ./wrt/files/usr/sbin/opencode-role-link
          cp -f ./files/usr/sbin/agent-tools-link ./wrt/files/usr/sbin/agent-tools-link
          cp -f ./files/usr/sbin/rclone-data-backup ./wrt/files/usr/sbin/rclone-data-backup
          cp -f ./files/usr/sbin/agent-runtime-auto-upgrade ./wrt/files/usr/sbin/agent-runtime-auto-upgrade
          cp -f ./files/usr/sbin/multica-log-hygiene ./wrt/files/usr/sbin/multica-log-hygiene
          cp -f ./files/usr/sbin/multica-runtime-guard ./wrt/files/usr/sbin/multica-runtime-guard
          cp -f ./files/usr/sbin/nikki-dns-failopen ./wrt/files/usr/sbin/nikki-dns-failopen
          chmod 0755 \
            ./wrt/files/etc/init.d/data-runtime \
            ./wrt/files/etc/init.d/emmc-data-provision \
            ./wrt/files/etc/init.d/rclone-data-backup \
            ./wrt/files/etc/init.d/nikki-dns-failopen \
            ./wrt/files/etc/uci-defaults/93-logd-ring-size \
            ./wrt/files/etc/uci-defaults/94-zram-swap-defaults \
            ./wrt/files/etc/uci-defaults/95-multica-maintenance-cron \
            ./wrt/files/etc/uci-defaults/98-provision-emmc-data \
            ./wrt/files/etc/uci-defaults/99-auto-mount-data \
            ./wrt/files/etc/uci-defaults/99-enable-data-runtime \
            ./wrt/files/etc/uci-defaults/99-enable-nikki-dns-failopen \
            ./wrt/files/usr/sbin/multica-device-profile \
            ./wrt/files/usr/sbin/emmc-data-provision \
            ./wrt/files/usr/sbin/auto-mount-data \
            ./wrt/files/usr/sbin/openwrt-data-storage-diagnose \
            ./wrt/files/usr/sbin/pi-append-system-link \
            ./wrt/files/usr/sbin/commandcode-role-link \
            ./wrt/files/usr/sbin/opencode-role-link \
            ./wrt/files/usr/sbin/agent-tools-link \
            ./wrt/files/usr/sbin/rclone-data-backup \
            ./wrt/files/usr/sbin/agent-runtime-auto-upgrade \
            ./wrt/files/usr/sbin/multica-log-hygiene \
            ./wrt/files/usr/sbin/multica-runtime-guard \
            ./wrt/files/usr/sbin/nikki-dns-failopen
          grep -q '^START=99$' ./wrt/files/etc/init.d/nikki-dns-failopen || {
            echo "ERROR: nikki-dns-failopen must use START=99" >&2
            exit 1
          }
          test -x ./wrt/files/usr/sbin/openwrt-data-storage-diagnose || {
            echo "ERROR: read-only data storage diagnosis helper is missing" >&2
            exit 1
          }
          test -x ./wrt/files/etc/init.d/data-runtime || {
            echo "ERROR: data runtime init script is missing" >&2
            exit 1
          }

      - name: Inject Private Firmware Configuration
        if: inputs.WRT_FEATURE_OVERLAY == true
        env:
          HEADSCALE_OPENWRT_AUTHKEY: ${{secrets.HEADSCALE_OPENWRT_AUTHKEY}}
          MULTICA_TOKEN: ${{secrets.MULTICA_TOKEN}}
          MULTICA_SERVER_URL: ${{secrets.MULTICA_SERVER_URL}}
          MULTICA_APP_URL: ${{secrets.MULTICA_APP_URL}}
          MULTICA_WORKSPACE_ID: ${{secrets.MULTICA_WORKSPACE_ID}}
          OPENWRT_DROPBEAR_AUTHORIZED_KEYS: ${{secrets.OPENWRT_DROPBEAR_AUTHORIZED_KEYS}}
          WRTBAK_R2_ENDPOINT: ${{secrets.WRTBAK_R2_ENDPOINT}}
          WRTBAK_R2_REGION: ${{secrets.WRTBAK_R2_REGION}}
          WRTBAK_R2_BUCKET: ${{secrets.WRTBAK_R2_BUCKET}}
          WRTBAK_R2_PREFIX: ${{secrets.WRTBAK_R2_PREFIX}}
          WRTBAK_R2_ACCESS_KEY_ID: ${{secrets.WRTBAK_R2_ACCESS_KEY_ID}}
          WRTBAK_R2_SECRET_ACCESS_KEY: ${{secrets.WRTBAK_R2_SECRET_ACCESS_KEY}}
          WRTBAK_HOME_PROXY_URL: ${{secrets.WRTBAK_HOME_PROXY_URL}}
          WRTBAK_OFFICE_PROXY_URL: ${{secrets.WRTBAK_OFFICE_PROXY_URL}}
          WRTBAK_DORM_PROXY_URL: ${{secrets.WRTBAK_DORM_PROXY_URL}}
          COMMANDCODE_API_KEY: ${{secrets.COMMANDCODE_API_KEY}}
          CLIPROXYAPI_API_KEY: ${{secrets.CLIPROXYAPI_API_KEY}}
          CLIPROXYAPI_BASE_URL: ${{secrets.CLIPROXYAPI_BASE_URL}}
        run: |
            guard_env="$(mktemp)"
            trap 'rm -f "$guard_env"' EXIT
            credential_supplied=false
            for credential_name in \
              HEADSCALE_OPENWRT_AUTHKEY MULTICA_TOKEN \
              WRTBAK_R2_ACCESS_KEY_ID WRTBAK_R2_SECRET_ACCESS_KEY \
              WRTBAK_HOME_PROXY_URL WRTBAK_OFFICE_PROXY_URL WRTBAK_DORM_PROXY_URL \
              COMMANDCODE_API_KEY CLIPROXYAPI_API_KEY CLIPROXYAPI_BASE_URL; do
              [ -z "${!credential_name:-}" ] || credential_supplied=true
            done
            $GITHUB_WORKSPACE/Scripts/HeadscaleAutoEnroll.sh "$GITHUB_WORKSPACE/wrt/files"
            $GITHUB_WORKSPACE/Scripts/MulticaAutoEnroll.sh "$GITHUB_WORKSPACE/wrt/files"
            $GITHUB_WORKSPACE/Scripts/CommandCodeProviderConfig.sh "$GITHUB_WORKSPACE/wrt/files"
            bash "$GITHUB_WORKSPACE/Scripts/PiCliProxyApiProviderConfig.sh" "$GITHUB_WORKSPACE/wrt/files"
            $GITHUB_WORKSPACE/Scripts/DropbearAuthorizedKeys.sh "$GITHUB_WORKSPACE/wrt/files"
            $GITHUB_WORKSPACE/Scripts/WrtbakR2Config.sh "$GITHUB_WORKSPACE/wrt/files"
            $GITHUB_WORKSPACE/Scripts/PrivateFirmwareGuard.sh "$GITHUB_WORKSPACE/wrt/files" > "$guard_env"
            [ "$(grep -c '^WRT_PRIVATE_BUILD=' "$guard_env")" -eq 1 ]
            [ "$(grep -c '^WRT_PRIVATE_BUILD_REASON=' "$guard_env")" -eq 1 ]
            [ "$(grep -c '^WRT_ARTIFACT_PRIVACY_SUFFIX=' "$guard_env")" -eq 1 ]
            if [ "$credential_supplied" = true ]; then
              grep -qx 'WRT_PRIVATE_BUILD=true' "$guard_env" || {
                echo "ERROR: injected credentials were not classified as private firmware" >&2
                exit 1
              }
            fi
            if grep -qx 'WRT_PRIVATE_BUILD=true' "$guard_env"; then
              grep -qx 'WRT_ARTIFACT_PRIVACY_SUFFIX=private' "$guard_env"
            else
              grep -qx 'WRT_PRIVATE_BUILD=false' "$guard_env"
              grep -qx 'WRT_ARTIFACT_PRIVACY_SUFFIX=public' "$guard_env"
            fi
            cat "$guard_env" >> "$GITHUB_ENV"

      - name: Custom Settings
        run: |
          . $GITHUB_WORKSPACE/Scripts/function.sh
          cd ./wrt/

          generate_config && cat .config

          if [ "${WRT_CONTAINER_RUNTIME_RESOLVED_MODE:-}" = "openwrt" ]; then
            cat >> .config <<'EOF'
          CONFIG_PACKAGE_containerd=y
          CONFIG_PACKAGE_nerdctl=y
          CONFIG_PACKAGE_runc=y
          EOF
          fi

          if [ "${WRT_FEATURE_OVERLAY:-true}" != "true" ]; then
            sed -i -E '/^CONFIG_PACKAGE_(luci-app-lucky|luci-app-tailscale-community|luci-app-wrtbak|tailscale)=/d' .config
            cat >> .config <<'EOF'
          # CONFIG_PACKAGE_luci-app-lucky is not set
          # CONFIG_PACKAGE_luci-app-tailscale-community is not set
          # CONFIG_PACKAGE_luci-app-wrtbak is not set
          # CONFIG_PACKAGE_tailscale is not set
          EOF
            if [ "${WRT_TAILSCALE_ROUTE_RECONCILE:-true}" = "true" ]; then
              cat >> .config <<'EOF'
          CONFIG_PACKAGE_tailscale=y
          CONFIG_PACKAGE_luci-app-tailscale-community=y
          EOF
            fi
          fi

          $GITHUB_WORKSPACE/Scripts/Settings.sh

          make defconfig -j$(nproc)
          # zram-swap only sets a UCI preference.  Verify that the target
          # kernel actually exposes lz4 before building an image; otherwise
          # the device silently starts with its lzo-rle fallback.
          for zram_config in \
            CONFIG_PACKAGE_zram-swap=y \
            CONFIG_PACKAGE_kmod-zram=y \
            CONFIG_PACKAGE_kmod-lib-lz4=y \
            CONFIG_KERNEL_ZRAM_BACKEND_LZ4=y \
            CONFIG_KERNEL_ZRAM_DEF_COMP_LZ4=y; do
            grep -Fxq "$zram_config" .config || {
              echo "ERROR: required zram lz4 configuration was dropped by defconfig: $zram_config" >&2
              exit 1
            }
          done
          if [ "${WRT_CONTAINER_RUNTIME_RESOLVED_MODE:-}" = "openwrt" ]; then
            for package in containerd nerdctl runc; do
              grep -q "^CONFIG_PACKAGE_${package}=y$" .config || {
                echo "ERROR: container runtime test package was not retained: ${package}" >&2
                exit 1
              }
            done
          elif [ "${WRT_CONTAINER_RUNTIME_RESOLVED_MODE:-}" = "prebuilt" ]; then
            for binary in \
              ./files/usr/bin/containerd \
              ./files/usr/bin/containerd-shim-runc-v2 \
              ./files/usr/bin/ctr \
              ./files/usr/bin/nerdctl \
              ./files/usr/sbin/runc; do
              test -x "$binary" || {
                echo "ERROR: prebuilt container runtime binary is missing: $binary" >&2
                exit 1
              }
            done
          fi
          if [ "${WRT_FEATURE_OVERLAY:-true}" = "true" ] || [ "${WRT_TAILSCALE_ROUTE_RECONCILE:-true}" = "true" ]; then
            grep -q '^CONFIG_PACKAGE_tailscale=y$' .config || {
              echo "ERROR: tailscale was dropped from .config after defconfig"
              exit 1
            }
            grep -q '^CONFIG_PACKAGE_luci-app-tailscale-community=y$' .config || {
              echo "ERROR: luci-app-tailscale-community was dropped from .config after defconfig"
              exit 1
            }
          fi
          if [ "${WRT_FEATURE_OVERLAY:-true}" = "true" ]; then
            grep -q '^CONFIG_PACKAGE_luci-app-wrtbak=y$' .config || {
              echo "ERROR: luci-app-wrtbak was dropped from .config after defconfig"
              exit 1
            }
          fi
          make clean -j$(nproc)

      - name: Guard Expected Device Config
        if: inputs.WRT_EXPECTED_DEVICE != ''
        run: |
          "$GITHUB_WORKSPACE/Scripts/GuardReCs07Artifact.sh" defconfig \
            "$GITHUB_WORKSPACE/wrt/.config" "$WRT_EXPECTED_DEVICE"

      - name: Download Packages
        if: env.WRT_TEST != 'true'
        run: |
          cd ./wrt/

          make download -j$(nproc)

      - name: Reserve Disk Space Before Compile
        if: env.WRT_TEST != 'true'
        env:
          WRT_CACHE_LOW_WATER_GB: ${{ env.WRT_CACHE_LOW_WATER_GB }}
          WRT_CACHE_TARGET_FREE_GB: ${{ env.WRT_CACHE_TARGET_FREE_GB }}
          WRT_CCACHE_MAX_SIZE: ${{ env.WRT_CCACHE_MAX_SIZE }}
          WRT_GO_BUILD_MAX: ${{ env.WRT_GO_BUILD_MAX }}
          WRT_GOMOD_MAX: ${{ env.WRT_GOMOD_MAX }}
        run: |
          set -euo pipefail
          # The early "Free Disk Space" action already reclaims hosted-only
          # bulk before Go/Node are installed. Here we never delete the active
          # AGENT_TOOLSDIRECTORY and never unconditionally clear ccache/Go.
          self_hosted=false
          case '${{ inputs.RUNNER_LABELS }}' in
            *self-hosted*) self_hosted=true ;;
          esac

          reserve_started_at=$(date +%s)
          echo "========disk before reserve========"
          df -h ./wrt || true

          bash "$GITHUB_WORKSPACE/Scripts/wrt_cache_reclaim.sh" discover-ccache \
            --wrt-dir "$GITHUB_WORKSPACE/wrt"
          bash "$GITHUB_WORKSPACE/Scripts/wrt_cache_reclaim.sh" reclaim \
            --wrt-dir "$GITHUB_WORKSPACE/wrt" \
            --self-hosted "$self_hosted" \
            --low-water-gb "$WRT_CACHE_LOW_WATER_GB" \
            --target-free-gb "$WRT_CACHE_TARGET_FREE_GB" \
            --ccache-max "$WRT_CCACHE_MAX_SIZE" \
            --go-build-max "$WRT_GO_BUILD_MAX" \
            --gomod-max "$WRT_GOMOD_MAX"

          echo "========disk after reserve========"
          df -h ./wrt || true
          echo "Reserve disk stage elapsed: $(( $(date +%s) - reserve_started_at ))s"

      - name: Compile Firmware
        id: compile_firmware
        if: env.WRT_TEST != 'true'
        run: |
          cd ./wrt/

          # Time the compile without swallowing the real exit code. The exact
          # parallel-then-serial fallback is preserved.
          compile_started_at=$(date +%s)
          set +e
          make -j$(nproc) || make -j1 V=s
          compile_rc=$?
          set -e
          echo "Compile stage elapsed: $(( $(date +%s) - compile_started_at ))s (rc=$compile_rc)"
          if [ "$compile_rc" -ne 0 ]; then
            exit "$compile_rc"
          fi

          "$GITHUB_WORKSPACE/Scripts/GuardAthenaLedArtifact.sh" \
            "$PWD/.config" "$PWD/bin/targets"

      - name: Record Compile Cache Stats
        if: env.WRT_TEST != 'true'
        run: |
          set -uo pipefail
          echo "========post-compile cache stats========"
          bash "$GITHUB_WORKSPACE/Scripts/wrt_cache_reclaim.sh" stats \
            --wrt-dir "$GITHUB_WORKSPACE/wrt"

      - name: Guard Build Cache Payload
        if: env.WRT_TEST != 'true'
        continue-on-error: true
        run: |
          set -euo pipefail
          # Advisory defense-in-depth: scans the cache whitelist for secret or
          # rootfs indicators before save. Non-blocking (continue-on-error) so
          # a false positive on a binary/regenerable cache cannot hold up a
          # build; offenders are logged as ::error:: and the save still runs.
          bash "$GITHUB_WORKSPACE/Scripts/wrt_cache_payload_guard.sh" manifest \
            --wrt-dir "$GITHUB_WORKSPACE/wrt"

      - name: Save Build Cache
        # Publish a new generation only for a successful, non-test, non-cancelled
        # build. The key is unique per run/attempt, so this never overwrites a
        # prior generation and no entry is deleted to make room.
        if: ${{ !cancelled() && steps.compile_firmware.outcome == 'success' && env.WRT_TEST != 'true' }}
        id: cache_save
        continue-on-error: true
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          # Identical allow-list to the restore step / payload guard.
          path: |
            ./wrt/.ccache
            ./wrt/staging_dir/host
            ./wrt/staging_dir/toolchain-*
            ./wrt/dl/go-mod-cache
            ./wrt/tmp/go-build
          key: ${{ env.WRT_CACHE_SAVE_KEY }}

      - name: Report Cache Save Result
        if: ${{ !cancelled() && env.WRT_TEST != 'true' }}
        env:
          SAVE_OUTCOME: ${{ steps.cache_save.outcome }}
          SAVE_KEY: ${{ env.WRT_CACHE_SAVE_KEY }}
        run: |
          set -uo pipefail
          echo "========cache save result========"
          echo "save key: $SAVE_KEY"
          du -sh ./wrt/.ccache ./wrt/staging_dir/host ./wrt/staging_dir/toolchain-* \
            ./wrt/dl/go-mod-cache ./wrt/tmp/go-build 2>/dev/null || true
          echo "actions/cache/save step outcome: ${SAVE_OUTCOME:-<not-run>}"
          if [ "${SAVE_OUTCOME:-}" = "success" ]; then
            echo "save step finished; confirm the action log line 'Cache saved' and its size for the actual upload."
            echo "NOTE: a green save step is necessary but not proof of a usable remote cache entry."
          else
            echo "::warning::no new cache generation was reliably published (outcome=${SAVE_OUTCOME:-<not-run>}); the previously restored cache was left untouched (nothing deleted)."
          fi

      - name: Machine Information
        run: |
          cd ./wrt/

          echo "======================="
          lscpu | grep -E "name|Core|Thread"
          echo "======================="
          df -h
          echo "======================="
          du -h --max-depth=1
          echo "======================="

      - name: Package Firmware
        run: |
          cd ./wrt/ && mkdir ./upload/

          cp -f ./.config ./upload/Config-"$WRT_CONFIG"-"$WRT_INFO"-"$WRT_BRANCH"-"$WRT_DATE".txt

          if [[ $WRT_TEST != 'true' ]]; then
            echo "WRT_KVER=$(find ./bin/targets/ -type f -name "*.manifest" -exec grep -oP '^kernel - \K[\d\.]+' {} \;)" >> $GITHUB_ENV
            echo "WRT_LIST=$(find ./bin/targets/ -type f -name "*.manifest" -exec grep -oP '^luci-(app|theme)[^ ]*' {} \; | tr '\n' ' ')" >> $GITHUB_ENV

            if [[ -n "$WRT_EXPECTED_DEVICE" ]]; then
              "$GITHUB_WORKSPACE/Scripts/GuardReCs07Artifact.sh" stage \
                ./bin/targets ./upload "$WRT_EXPECTED_DEVICE"
            else
              find ./bin/targets/ -iregex ".*\(buildinfo\|json\|sha256sums\|packages\)$" -exec rm -rf {} +

              for FILE in $(find ./bin/targets/ -type f -iname "*$WRT_TARGET*") ; do
                EXT=$(basename $FILE | cut -d '.' -f 2-)
                NAME=$(basename $FILE | cut -d '.' -f 1 | grep -io "\($WRT_TARGET\).*")
                NEW_FILE="$NAME"-"$WRT_DATE"."$EXT"
                mv -f $FILE ./upload/$NEW_FILE
              done

              find ./bin/targets/ -type f -exec mv -f {} ./upload/ \;
            fi

            if [ -n "${WRT_REQUIRED_DEVICE:-}" ]; then
              find ./upload -maxdepth 1 -type f -name "*${WRT_REQUIRED_DEVICE}*factory*.bin" | grep -q . || {
                echo "ERROR: missing ${WRT_REQUIRED_DEVICE} factory image" >&2
                exit 1
              }
              find ./upload -maxdepth 1 -type f -name "*${WRT_REQUIRED_DEVICE}*sysupgrade*.bin" | grep -q . || {
                echo "ERROR: missing ${WRT_REQUIRED_DEVICE} sysupgrade image" >&2
                exit 1
              }
            fi

            jq -n \
              --arg source_repository "$WRT_REPO" \
              --arg source_branch "$WRT_BRANCH" \
              --arg source_commit "$WRT_HASH" \
              --arg config "$WRT_CONFIG" \
              --arg required_device "$WRT_REQUIRED_DEVICE" \
              --arg feature_overlay "$WRT_FEATURE_OVERLAY" \
              --arg route_reconcile "$WRT_TAILSCALE_ROUTE_RECONCILE" \
              --arg container_runtime_mode "${WRT_CONTAINER_RUNTIME_RESOLVED_MODE:-disabled}" \
              '{source_repository:$source_repository,source_branch:$source_branch,source_commit:$source_commit,config:$config,required_device:$required_device,feature_overlay:$feature_overlay,route_reconcile:$route_reconcile,container_runtime_mode:$container_runtime_mode}' \
              > ./upload/metadata.json
            if [ -f ./files/usr/share/container-runtime/versions ]; then
              cp -f ./files/usr/share/container-runtime/versions ./upload/ContainerRuntime-versions.txt
            fi
            (cd ./upload && find . -maxdepth 1 -type f ! -name SHA256SUMS -printf '%f\n' | LC_ALL=C sort | xargs sha256sum > SHA256SUMS)
            if [[ -n "$WRT_EXPECTED_DEVICE" ]]; then
              "$GITHUB_WORKSPACE/Scripts/GuardReCs07Artifact.sh" verify \
                ./upload "$WRT_EXPECTED_DEVICE"
            fi

            make clean -j$(nproc)
          fi

      - name: Split Public Firmware Artifacts By Device
        if: inputs.WRT_SPLIT_DEVICE_ARTIFACTS == true && env.WRT_PRIVATE_BUILD != 'true' && env.WRT_BUILD_ONLY != 'true' && env.WRT_TEST != 'true'
        run: |
          cd ./wrt/
          "$GITHUB_WORKSPACE/Scripts/SplitFirmwareArtifacts.sh" ./upload ./artifact-groups

      - name: Upload Firmware Artifact
        if: inputs.WRT_SPLIT_DEVICE_ARTIFACTS == false || env.WRT_PRIVATE_BUILD == 'true' || env.WRT_BUILD_ONLY == 'true' || env.WRT_TEST == 'true'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ${{env.CI_NAME}}-${{env.WRT_CONFIG}}_${{env.WRT_DATE}}-${{env.WRT_ARTIFACT_PRIVACY_SUFFIX}}
          path: ./wrt/upload/
          if-no-files-found: error
          retention-days: 14

      - name: Upload jdcloud_re-ss-01 Firmware Artifact
        if: inputs.WRT_SPLIT_DEVICE_ARTIFACTS == true && env.WRT_PRIVATE_BUILD != 'true' && env.WRT_BUILD_ONLY != 'true' && env.WRT_TEST != 'true'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ${{env.CI_NAME}}-${{env.WRT_CONFIG}}_${{env.WRT_DATE}}-public-jdcloud_re-ss-01
          path: ./wrt/artifact-groups/jdcloud_re-ss-01/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload jdcloud_re-cs-02 Firmware Artifact
        if: inputs.WRT_SPLIT_DEVICE_ARTIFACTS == true && env.WRT_PRIVATE_BUILD != 'true' && env.WRT_BUILD_ONLY != 'true' && env.WRT_TEST != 'true'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ${{env.CI_NAME}}-${{env.WRT_CONFIG}}_${{env.WRT_DATE}}-public-jdcloud_re-cs-02
          path: ./wrt/artifact-groups/jdcloud_re-cs-02/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload jdcloud_re-cs-07 Firmware Artifact
        if: inputs.WRT_SPLIT_DEVICE_ARTIFACTS == true && env.WRT_PRIVATE_BUILD != 'true' && env.WRT_BUILD_ONLY != 'true' && env.WRT_TEST != 'true'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ${{env.CI_NAME}}-${{env.WRT_CONFIG}}_${{env.WRT_DATE}}-public-jdcloud_re-cs-07
          path: ./wrt/artifact-groups/jdcloud_re-cs-07/
          if-no-files-found: ignore
          retention-days: 14

      - name: Upload Other Device Firmware Artifact
        if: inputs.WRT_SPLIT_DEVICE_ARTIFACTS == true && env.WRT_PRIVATE_BUILD != 'true' && env.WRT_BUILD_ONLY != 'true' && env.WRT_TEST != 'true'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ${{env.CI_NAME}}-${{env.WRT_CONFIG}}_${{env.WRT_DATE}}-public-other-devices
          path: ./wrt/artifact-groups/other-devices/
          if-no-files-found: ignore
          retention-days: 14

      - name: Release Firmware
        if: env.WRT_PRIVATE_BUILD != 'true' && env.WRT_BUILD_ONLY != 'true' && env.WRT_TEST != 'true'
        env:
          GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
        run: |
          . $GITHUB_WORKSPACE/Scripts/retry.sh
          TAG="${CI_NAME}-${WRT_CONFIG}_${WRT_DATE}"
          RELEASE_PREFIX="${CI_NAME}-${WRT_CONFIG}_"
          NOTES_FILE="$RUNNER_TEMP/release-notes.txt"

          cat > "$NOTES_FILE" <<EOF
          这是个平台固件包，内含多个设备！
          请注意选择你需要的设备固件！
          不要问，刷就完事了！

          全系带开源硬件加速，别问了！

          源码：${WRT_REPO}
          分支：${WRT_BRANCH}
          提交：${WRT_HASH}

          配置：${WRT_CONFIG}
          平台：${WRT_TARGET}

          登录地址：${WRT_IP}
          登录密码：${WRT_PW}

          WIFI名称：${WRT_SSID}

          内核版本：${WRT_KVER}
          插件列表：${WRT_LIST}
          EOF

          cd ./wrt/upload
          assets=(./*)
          [ -e "${assets[0]}" ] || {
            echo "ERROR: no release assets found in ./wrt/upload"
            exit 1
          }

          while IFS= read -r old_tag; do
            [ -n "$old_tag" ] || continue
            case "$old_tag" in
              "${RELEASE_PREFIX}"*)
                echo "Deleting old release: $old_tag"
                retry_cmd 5 15 gh release delete -R "$GITHUB_REPOSITORY" "$old_tag" --yes --cleanup-tag
                ;;
            esac
          done < <(gh release list -R "$GITHUB_REPOSITORY" --limit 1000 --json tagName --jq '.[].tagName')

          retry_cmd 5 15 gh release create -R "$GITHUB_REPOSITORY" "$TAG" --title "$TAG" --notes-file "$NOTES_FILE"

          for asset in "${assets[@]}"; do
            retry_cmd 5 15 gh release upload -R "$GITHUB_REPOSITORY" "$TAG" "$asset" --clobber
          done

      # ------------------------------------------------------------------
      # CI Debug Gate: failure of custom_packages/compile_firmware, or
      # explicit DEBUG_SSH=true, enrolls the runner into the Headless
      # Tailnet (tag:ci-debug) with Tailscale SSH and holds it for up to
      # 90 minutes. Release by: touch /tmp/continue-ci
      # ------------------------------------------------------------------
      - name: CI Debug Gate (Tailscale SSH)
        id: ci_debug_gate
        if: >-
          always() && (
            env.DEBUG_SSH == 'true' ||
            steps.custom_packages.outcome == 'failure' ||
            steps.compile_firmware.outcome == 'failure'
          )
        continue-on-error: true
        shell: bash
        env:
          HEADSCALE_AUTHKEY: ${{ secrets.HEADSCALE_CI_AUTHKEY }}
          HEADSCALE_LOGIN: ${{ secrets.HEADSCALE_URL }}
        run: |
          set -uo pipefail
          echo "Debug gate triggered: DEBUG_SSH=${DEBUG_SSH:-} custom_packages=${{ steps.custom_packages.outcome }} compile_firmware=${{ steps.compile_firmware.outcome }}"
          if [ -z "${HEADSCALE_AUTHKEY:-}" ] || [ -z "${HEADSCALE_LOGIN:-}" ]; then
            echo "WARN: HEADSCALE_CI_AUTHKEY / HEADSCALE_URL secrets missing; skipping Tailscale debug gate"
            exit 0
          fi
          DEBUG_ENV_FILE="${RUNNER_TEMP}/ci-debug-env"
          cleanup_ci_debug() {
            local rc=$?
            bash "$GITHUB_WORKSPACE/Scripts/cleanup_ci_tailscale.sh"
            rm -f "$DEBUG_ENV_FILE" /tmp/continue-ci
            return "$rc"
          }
          trap cleanup_ci_debug EXIT
          rm -f "$DEBUG_ENV_FILE"
          export CI_DEBUG_ENV_FILE="$DEBUG_ENV_FILE"
          # Invoke through bash as a defensive fallback for checkouts where a
          # platform or archive transport loses the executable mode bit.
          if ! bash "$GITHUB_WORKSPACE/Scripts/setup_ci_tailscale.sh"; then
            echo "WARN: Tailscale debug gate setup failed; continuing pipeline without remote SSH"
            exit 0
          fi
          if [ ! -s "$DEBUG_ENV_FILE" ]; then
            echo "WARN: Tailscale setup produced no same-step environment file; continuing pipeline without remote SSH"
            exit 0
          fi
          # GITHUB_ENV only applies to later steps; source the explicit file
          # emitted by setup_ci_tailscale.sh for this step.
          # shellcheck disable=SC1090
          source "$DEBUG_ENV_FILE"
          echo "========================================================================"
          echo "RUNNER HELD FOR REMOTE DEBUGGING (up to 90 minutes)"
          echo "Agent login (tag:ssh-admin side, any tailnet host):"
          echo "  ssh runner@${CI_TAILNET_IP}"
          echo "  ssh -o User=runner 100.64.0.${CI_TAILNET_OCTET}"
          echo "Env probe one-liner:"
          echo "  ssh runner@${CI_TAILNET_IP} 'bash ${GITHUB_WORKSPACE}/Scripts/ci-debug-probe.sh'"
          echo "Release the runner by running:  touch /tmp/continue-ci"
          echo "========================================================================"
          deadline=$(( $(date +%s) + 5400 ))
          while [ ! -e /tmp/continue-ci ]; do
            if [ "$(date +%s)" -ge "$deadline" ]; then
              echo "Debug gate timeout (90 min); releasing runner"
              break
            fi
            sleep 15
          done
          if [ -e /tmp/continue-ci ]; then
            echo "continue-ci detected; releasing runner"
          fi
          rm -f /tmp/continue-ci
          echo "CI resumed"
